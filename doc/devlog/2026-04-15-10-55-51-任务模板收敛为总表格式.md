# 任务模板收敛为总表格式

- 本次目标：
  - 将 `TASK.md` 进一步收敛为单一的“任务总表”维护方式。
- 核心改动：
  - 删除 `TASK.md` 中位于“新任务录入模板”下方的详细任务块。
  - 将新任务录入模板改为与“任务总表”完全一致的表头格式。
  - 保留当前活跃任务仅在总表中维护，避免同一任务出现两套结构。
- 修改文件：
  - `TASK.md`
- 校验情况：
  - 已使用 UTF-8 重新读取 `TASK.md`，确认中文和表格结构正常。
- 风险或遗留项：
  - 本次仅调整任务文档结构，未执行 `dotnet build` / `dotnet test`。
