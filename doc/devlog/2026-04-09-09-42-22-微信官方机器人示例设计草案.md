# 本次目标

为 `weixin-bot-sample` 仓库补齐微信官方 iLink Bot API 示例的一期需求与设计基线，明确后续开发范围。

# 核心改动

- 在 `TASK.md` 新增 `T002`，登记“微信官方 iLink Bot API 示例”任务，并将当前阶段标记为设计文档评审。
- 新增 `doc/requirement.md`，整理项目背景、范围、页面模块、核心流程、关键字段与异常场景。
- 新增 `doc/design/设计文档.md`，输出架构设计、功能需求、权限矩阵、ASCII 原型和交互说明。
- 明确一期范围复用 `C:\Work\github\OpenCowork` 中二维码绑定、长轮询收消息与文本回包的实现经验。

# 修改文件

- `TASK.md`
- `doc/requirement.md`
- `doc/design/设计文档.md`

# 校验情况

- 已校验：文档已按仓库规范补齐，任务状态与阶段描述一致。
- 未执行：`dotnet build`、`dotnet test`。
- 未执行原因：当前仓库尚未进入实现阶段，暂无可构建的 .NET 项目代码。

# 风险或遗留项

- 当前仍处于阶段 0，需用户确认设计文档后才能进入阶段 1 实现。
- 后续真实联调仍需要可用的微信 iLink 测试配置和账号环境。
