# 本次目标

- 修复首页中超长 `get_updates_buf` 和 `typing_ticket` 撑破卡片布局的问题。

# 核心改动

- 为首页中的超长协议字段增加 `technical-value` 样式，统一采用等宽字体、允许任意位置换行。
- 为卡片、提示区块和元信息行补充 `min-width: 0`，避免 Grid 子项因为长字符串拒绝收缩。
- 为长字段增加 `title`，方便悬停查看完整内容。

# 修改文件

- `src/WeixinBotSample/Components/Pages/Home.razor`
- `src/WeixinBotSample/wwwroot/css/app.css`
- `TASK.md`

# 校验情况

- 已执行 `dotnet build WeixinBotSample.slnx`
- 已执行临时启动 + 首页 smoke test，返回 `StatusCode=200`

# 风险或遗留项

- 本次主要修复卡片字段溢出；若后续媒体记录表格里出现更长的加密参数，仍建议对表格单元格做同样的技术字段样式处理。
