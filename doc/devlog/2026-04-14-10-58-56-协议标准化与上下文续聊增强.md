# 本次目标

- 依据 `doc/iLinkBot协议.md`，把现有微信 iLink Bot Demo 补齐为更标准的对接示例。

# 核心改动

- 服务层补齐标准请求头、`base_info.channel_version`、`get_updates_buf` 游标持久化、`getconfig` / `sendtyping`、`errcode=-14` 会话过期处理与长轮询失败退避。
- 新增连接自检、Typing Ticket 缓存、已知联系人与 `context_token` 缓存，让主动推送可基于最近联系人续聊。
- 首页增加协议自检与上下文缓存展示，补充协议行为单元测试，并同步更新需求、设计和 README 文档。

# 修改文件

- `src/WeixinBotSample/Services/WeixinBotDemoService*.cs`
- `src/WeixinBotSample/Models/*.cs`
- `src/WeixinBotSample/Components/Pages/Home.razor*`
- `tests/WeixinBotSample.Tests/WeixinBotDemoServiceProtocolTests.cs`
- `doc/requirement.md`
- `doc/design/设计文档.md`
- `README.md`
- `TASK.md`

# 校验情况

- 已执行 `dotnet build WeixinBotSample.slnx`
- 已执行 `dotnet test WeixinBotSample.slnx`
- 已执行 `dotnet publish src/WeixinBotSample/WeixinBotSample.csproj -c Release`
- 已执行临时启动 + 首页 smoke test，返回 `StatusCode=200`，页面包含“微信 iLink Bot 演示 Demo”

# 风险或遗留项

- `doc/iLinkBot协议.md` 仍是用户提供的转录文档，不是官方站点直连结果；后续若官方协议字段变化，需要再对照更新。
- 当前 Demo 仍聚焦文本消息链路，媒体上传下载、群聊能力与多账号管理未纳入本次实现。
